// reduce

let nums = [10, 20, 30, 40, 50];

// let totalSum = 0;
// nums.forEach((item) => {
//   totalSum = totalSum + item;
// });
// console.log(totalSum);

// reduce
// array.reduce(callback function, initialValue)

// const sum = nums.reduce((totalSum, item) => {
//   totalSum = totalSum + item;

//   return totalSum;
// }, 0);

// console.log(sum);

// let scoreList = [1, 2, 3, 4, 5];

// const result = scoreList.reduce((product, item, index, array) => {
//   product = product * item;

//   return product;
// }, 1);

// ? one liner
// const result = scoreList.reduce((product, item) => product * item, 1);
// console.log(result);
// ? product of all numbers

// ? includes
// ? results in boolean
// ? check if certain value is an element of array or not
// let studentList = ['Rohan', 'Roshan', 'Roman', 'Rom'];

// const hasGotRohan = studentList.includes('Rohan');

// console.log(hasGotRohan);

// console.log(studentList.join());
// console.log(studentList.join(' '));
// console.log(studentList.join('-'));
